const dotenv = require('dotenv')
dotenv.config()
const config = {
    serverURL: 'https://auto-trader-arg.herokuapp.com/',
    //serverURL: 'https://localhost:5000',
    //serverURL: '',
}

export default config